services:
  splitter:
    build: .
    # volumes:
    #   # Положите непустой certs/ca-bundle.crt (с корпоративными сертификатами)
    #   # чтобы переопределить системный CA-бандл.
    #   - ./certs/ca-bundle.crt:/etc/ssl/certs/ca-certificates.crt:ro
